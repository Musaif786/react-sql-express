mysql with react js

ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'your_current_password';

ye uper ka query mysql ke work bench me jo table ka naam diye usme jakar run karna jub ich data upload hota

------
server file me dalna ye necha ka code 
const express = require("express");
const app = express();
const bodyParser = require("body-parser");
const cors = require("cors");

app.use(cors());
app.use(express.json());
app.use(bodyParser.urlencoded({extended: true}));


const mysql = require("mysql");


const db = mysql.createConnection({
    host: "localhost",
    user: "root",
    password: "Musaif@8143",
    database: "cruddatabase",
});

app.get("/", (req,res)=>{
    // const sqlInsert = "insert into movie_reviews(moviename,moviereview) values('king','very good awesome and awesome');"
    db.query("INSERT INTO movie_reviews (moviename,moviereview) values('king','very good awesome and awesome');", (err,result) => {
       console.log(err)
        res.send("data stored into done database   "+ result);
        
    });
})

app.listen(3001, ()=>{
    console.log("3001 server running")
})